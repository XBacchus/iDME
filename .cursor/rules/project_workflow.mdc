---
description: 项目开发流程
globs: 
alwaysApply: true
---
# 开发流程

## 1. 预读
- 开始开发前先阅读 `.memories/general/*` 与 `.memories/modules/*`。

## 2. API 闭环
1. 新需求先写 `API_DOCS/demanding_api.json`（日期 + `unimplemented`）。
2. 实现后移除 demand 项并写入对应 API 文档 JSON。
3. 最终汇总更新 `API_DOCS/api_doc.json`。
4. API JSON 禁止注释。
5. 联调或发布前同步运行态 OpenAPI 快照到 `_verify/api-docs.json`（建议来源：`/v3/api-docs`）。

## 3. 必须同步文档
- 文件结构变化：更新 `.cursor/project_structure.md`
- 数据结构变化：更新 `.cursor/database_datainfo.md`

## 4. 环境约束
- 后端目录：`miniapp/backend`
- 前端目录：`miniapp/frontend`
- 端口默认以配置文件为准（当前后端 8080）
- 命令需兼容 PowerShell，避免 `&&`
